[project]
name = "pydirectus"
version = "0.2.0"
description = "A simple REST API Wrapper for Directus"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx",
]
[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"
